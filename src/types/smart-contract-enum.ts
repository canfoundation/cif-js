export enum CodeRightHolder {
  CODE = 0,
  AMENDMENT,
}

export enum ExecutionType {
  SOLE_DECISION = 0,
  COLLECTIVE_DECISION,
  BOTH,
}

export enum ApprovalType {
  SOLE_APPROVAL = 0,
  APPROVAL_CONSENSUS,
  BOTH_TYPE,
}

export enum ProposalStatus {
  IN_PROGRESS = 0,
  PROPOSAL_APPROVE,
  EXECUTED,
  EXPIRED,
}

export enum FillingType {
  APPOINTMENT = 0,
  ELECTION,
}

export enum VoteStatus {
  REJECT = 0,
  VOTE,
}

export enum CodeTypeEnum {
  NORMAL = 0,
  POSITION_CONFIG,
  POSITION_APPOINT,
  POSITION_DISMISS,
  BADGE_CONFIG,
  BADGE_ISSUE,
  AMENDMENT,
}
